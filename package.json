{
  "name": "csi-club-root",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "npm --prefix ./csi-nexus-main run dev",
    "build": "npm --prefix ./csi-nexus-main run build",
    "build:dev": "npm --prefix ./csi-nexus-main run build:dev",
    "lint": "npm --prefix ./csi-nexus-main run lint",
    "preview": "npm --prefix ./csi-nexus-main run preview",
    "test": "npm --prefix ./csi-nexus-main run test",
    "test:watch": "npm --prefix ./csi-nexus-main run test:watch"
  }
}
